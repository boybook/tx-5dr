/**
 * Callsign normalization utilities
 *
 * Re-exported from @tx5dr/plugin-api for backward compatibility with
 * existing server code that imports from this path.
 */
export { normalizeCallsign } from '@tx5dr/plugin-api';
