/**
 * Compatibility re-export. The standard QSO runtime is public so external
 * contest plugins can use the same implementation as this built-in strategy.
 */
export * from '@tx5dr/plugin-api/ft8';
